# Database (Required)
DB_HOST=localhost
DB_PORT=3306
DB_USER=billfold_dev
DB_PASS=dev_password
DB_DATABASE=billfold

# Redis (Optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASS=

# Cloudflare KV (Optional)
CF_ACCOUNT_ID=
CF_NAMESPACE_ID=
CF_API_TOKEN=

# Security (Optional)
JWT_SECRET=your-dev-secret-key-change-in-production
REDIRECT_WHITELIST=*.localhost

# Server
PORT=3001
NODE_ENV=development
